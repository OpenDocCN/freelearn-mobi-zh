- en: Activity Lifecycle
  id: totrans-0
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: '`Activity`生命周期'
- en: Every `Activity` within an app runs within the App Process, but each `Activity`
    is also subject to its own lifecycle. These are methods that are triggered by
    the platform when the `Activity` is about to change states, for example, when
    the user exits the `Activity` temporarily to use another `Activity` (whether it's
    in the same application, or another one). `Fragment` objects are also subject
    to a lifecycle, and while it mostly follows the same pattern as the `Activity`
    lifecycle, it also has the notion of being "attached" and "detached" from its
    parent `Activity`.
  id: totrans-1
  prefs: []
  type: TYPE_NORMAL
  zh: 应用程序中的每一个`Activity`都在App进程中运行，但每个`Activity`也都有自己的生命周期。这些方法是在`Activity`即将改变状态时由平台触发的，例如，当用户暂时退出`Activity`去使用另一个`Activity`（无论它是在同一应用程序中，还是另一个应用程序中）时。`Fragment`对象也受到生命周期的约束，虽然它主要遵循与`Activity`生命周期相同的模式，但它也有从其父`Activity`中“附加”和“分离”的概念。
- en: 'The following workflow explains the `Activity` lifecycle in detail:'
  id: totrans-2
  prefs: []
  type: TYPE_NORMAL
  zh: 下面的流程图详细解释了`Activity`的生命周期：
- en: '![](img/4dafc4c3-acbe-4094-8a74-42f86db92d15.png)'
  id: totrans-3
  prefs: []
  type: TYPE_IMG
  zh: '![图片](img/4dafc4c3-acbe-4094-8a74-42f86db92d15.png)'
